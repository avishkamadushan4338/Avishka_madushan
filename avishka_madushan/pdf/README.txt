Drop project PDFs here (e.g. case-study reports).
To activate the Download PDF button for a project, add the file here and set that project's `pdf` field in the index.html projectData object to the relative path, e.g. `pdf/ravana-report.pdf`.
